# Look in the main/ directory for everything
INPUT            = main

# Pick up all C sources and headers
FILE_PATTERNS    = *.c *.h

# Descend into sub-folders beneath main/
RECURSIVE        = YES

# Where to put output (relative to the project root)
OUTPUT_DIRECTORY = build/docs

# Make sure HTML output is enabled
GENERATE_HTML    = YES
